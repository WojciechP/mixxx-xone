
const pq = require('../src/mixxxbase')


it('foos', () => {
    console.log(pq)
})
/*
describe('control', () => {
    console.log(pq)
    const c = new pq.Control('Foo', 'bar')
    it('has stuff', () => {
        expect(c.group).toBe('Foo')
    })
})

describe('deck', () => {
    const d = new pq.Deck('[Channel2]');
    it('has stuff', () => {
        expect(d.group).toBe('[Channel2]');
    })
})

*/